A multinomial Bayes classifier for English text. Short, and sweet.

Here:
  from multibayes import MultinomialBayes
  training_set = (('some sentence example', 'its label'), ('another sentence', 'and its label'))
  m = MultinomialBayes(training_set)
  m.classify('a test example')
